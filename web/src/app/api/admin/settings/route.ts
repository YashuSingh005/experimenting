import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ message: "Settings API coming soon" }, { status: 501 });
}

export async function PUT() {
  return NextResponse.json({ message: "Settings API coming soon" }, { status: 501 });
}
